version: "2"

run:
  timeout: 5m

formatters:
  enable:
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused
    - gocritic
    - revive
    - errname
    - errorlint
    - exhaustive
    - goconst
    - godot
    - misspell
    - noctx
    - prealloc
    - unconvert
    - unparam
    - wastedassign
  settings:
    revive:
      rules:
        - name: unexported-return
          disabled: true
        - name: var-naming
          disabled: true
    gocritic:
      enabled-tags:
        - diagnostic
        - style
        - performance
      disabled-checks:
        - hugeParam
        - rangeValCopy
        - whyNoLint
        - httpNoBody
        - nestingReduce
        - unnamedResult
        - appendCombine
    exhaustive:
      default-signifies-exhaustive: true
    goconst:
      ignore-tests: true

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
