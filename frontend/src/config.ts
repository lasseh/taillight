// Feature flags — must match api/config.yml features section.
// See: api/config.yml → features
// CI check: make verify-features
export const features = {
  netlog: true,
  srvlog: true,
  applog: true,
}
